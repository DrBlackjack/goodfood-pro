import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'goodfood-pro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
